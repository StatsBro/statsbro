@model Site
@{
    Layout = "_Layout";
}
<div class="pagetitle">
    <h1>Eksport danych @Model.Domain</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-controller="Site" asp-action="Index">Strony</a></li>
            <li class="breadcrumb-item active">Eksport danych @Model.Domain</li>
        </ol>
    </nav>
</div><!-- End Page Title -->

<div class="card col-12 col-md-6">
    <div class="card-body">
        <h5 class="card-title">Eksport danych dla strony @Model.Domain</h5>
        <form class="row g-3" method="get" target="_blank" asp-action="DataExportFile" asp-route-siteid="@Model.Id">
            <div class="col-12">
                <label for="inputDate" class="col-sm-2 col-form-label">Dzień od</label>
                <div class="col-sm-10">
                    <input type="date" class="form-control" name="From" required>
                </div>
            </div>

            <div class="col-12">
                <label for="inputDate" class="col-sm-2 col-form-label">Dzień do</label>
                <div class="col-sm-10">
                    <input type="date" class="form-control" name="To" required>
                </div>
            </div>

            <div class="col-12">
                <input type="hidden" name="id" value="@Model.Id" />
                <button class="btn btn-primary" type="submit">Eksportuj</button>
            </div>

        </form>

        <div class="alert alert-info mt-3" role="alert">
            Eksport zawiera dane źródłowe o zdarzeniach.
            Ta opcja umożliwia eksport pierwszych 10 000 zdarzeń z wybranego okresu.
            Jeśli potrzebujesz wyeksportować większą ilość danych wystarczy, że prześlesz do nas taką prośbę.
        </div>
    </div>
</div>